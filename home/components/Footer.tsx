import React from "react";
import styles from "@/styles/Home.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      Footer component federated from home app
    </footer>
  );
};

export default Footer;
