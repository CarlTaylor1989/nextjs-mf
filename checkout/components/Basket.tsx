const Basket = () => {
  return (
    <div
      style={{
        display: "flex",
        width: "750px",
        gap: "20px",
        flexWrap: "wrap",
        marginTop: "25px"
      }}
    >
      This is the basket component from checkout app
    </div>
  );
};

export default Basket;
